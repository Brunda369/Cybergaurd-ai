table:
  name: honeypot_events
  schema: public
select_permissions:
  - role: user
    permission:
      columns:
        - id
        - ip
        - username
        - password
        - attempted_command
        - port
        - protocol
        - timestamp
      filter: {}
